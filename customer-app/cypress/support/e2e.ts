// 导入Cypress命令和断言
import './commands'
import '@testing-library/cypress/add-commands'

// 在测试执行前的全局设置
beforeEach(() => {
  // 可以在这里添加全局的测试前准备
})

// 在测试执行后的全局清理
afterEach(() => {
  // 可以在这里添加全局的测试后清理
})
